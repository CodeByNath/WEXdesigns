# `@weerax/adapters`

Framework-neutral boundary for future domain ownership adapters.

Adapters may depend on `@weerax/schemas`. They must not depend on React, Preact, UI components, WEX presentation, DOM APIs, or application shells. Adapter implementation has not started and requires a demonstrated domain contract.
