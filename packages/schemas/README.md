# `@weerax/schemas`

Framework-neutral, serializable runtime and TypeScript contracts for WeeraX Studios.

This foundation exposes only identity, semantic action, and WEX tier primitives already required by the architecture. It has no React, Vite, DOM, CSS, adapter, or UI dependency.
